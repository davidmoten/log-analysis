package org.moten.david.log.query;

/**
 * Aggregating function.
 * 
 * @author dave
 * 
 */
public enum Metric {
	FIRST, LAST, EARLIEST, LATEST, VARIANCE, VARIANCE_POPULATION, MEAN, MAX, MIN, MEDIAN, MODE, STANDARD_DEVIATION, SUM, SUM_SQUARES, COUNT;
}
